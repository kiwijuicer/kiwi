// This file is a temporary re-export layer kept only because the
// sandbox where this refactor ran cannot delete tracked files.
// Delete it locally with `rm packages/core/src/lifecycle/files.ts`
// once you pull the change.
export { readJson, writeJsonSafely } from "../storage/json-io";
