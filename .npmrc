# Use hoisted linker so node_modules layout is compatible with tsx path aliases
node-linker=hoisted

# Avoid prompts in CI / restricted environments
confirmModulesPurge=false

# Import strategy: copy (avoids hardlink/symlink issues on some filesystems)
package-import-method=copy
